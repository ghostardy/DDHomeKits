server:
  port: 8080
spring:
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
  datasource:
    url: jdbc:h2:file:./src/main/resources/db/DDHomeKitsDB
    driver-class-name: org.h2.Driver
    username: sa
    password:
    initialization-mode: always
    schema: classpath:db/schema.sql
